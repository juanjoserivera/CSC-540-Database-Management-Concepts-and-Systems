package test;

//import static org.junit.Assert.assertArrayEquals;

import java.time.LocalTime;
import java.util.Arrays;
import java.util.Collections;
import java.util.List;

//import org.junit.jupiter.api.Test;

import util.TimeSlot;

class TimeSlotTest {

//	@Test
//	void sort() {
//		List<TimeSlot> slots = Arrays.asList(new TimeSlot(LocalTime.of(13, 45), 2),
//				new TimeSlot(LocalTime.of(11, 30), 3), new TimeSlot(LocalTime.of(12, 30), 4));
//		Collections.sort(slots);
//		TimeSlot[] expectedTimeSlots = new TimeSlot[] { new TimeSlot(LocalTime.of(11, 30), 3),
//				new TimeSlot(LocalTime.of(12, 30), 4), new TimeSlot(LocalTime.of(13, 45), 2) };
//		assertArrayEquals(expectedTimeSlots, slots.toArray());
//	}

}
